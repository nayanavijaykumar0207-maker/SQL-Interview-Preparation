# WHERE Clause

## What is WHERE?

WHERE is used to filter data and show only the records that match a condition.

## Syntax

SELECT column_name
FROM table_name
WHERE condition;

## Example

SELECT *
FROM Customers
WHERE City = 'Bangalore';
